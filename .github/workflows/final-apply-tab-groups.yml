name: Disabled final tab workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled because the grouped tab logic is handled by final-safe-apply-tab-groups.yml"
